return {
  "MeanderingProgrammer/render-markdown.nvim",
  opts = {
    -- We keep the plugin enabled overall...
    code = {
      -- ...but we specifically disable the code block rendering component.
      enabled = false,
    },
  },
}
